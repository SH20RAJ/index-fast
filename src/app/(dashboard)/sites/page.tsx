import SitesView from "./SitesView";

export const metadata = {
  title: "Websites | IndexFast",
  description: "Manage your tracked sites and trigger manual indexing.",
};

export default function SitesPage() {
  return <SitesView />;
}
