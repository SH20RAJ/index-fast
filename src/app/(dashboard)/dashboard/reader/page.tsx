import ReaderView from "./ReaderView";

export const metadata = {
  title: "AI Content Reader | IndexFast",
  description: "Convert any webpage into clean Markdown for LLMs and SEO.",
};

export default function ReaderPage() {
  return <ReaderView />;
}
