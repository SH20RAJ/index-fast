import SettingsView from "./SettingsView";

export const metadata = {
  title: "Settings | IndexFast",
  description: "Update your account email and subscription details.",
};

export default function SettingsPage() {
  return <SettingsView />;
}
