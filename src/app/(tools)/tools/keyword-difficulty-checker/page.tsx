import ToolStaticPage, { createToolMetadata } from "@/components/tools/shared/tool-static-page";

export const metadata = createToolMetadata("keyword-difficulty-checker");

export default function KeywordDifficultyCheckerPage() {
  return <ToolStaticPage slug="keyword-difficulty-checker" />;
}
